apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    description: indexer-1-worker
  labels:
    app: indexer-1-worker
    environment: Development
    tier: Service
  name: indexer-1-worker
  namespace: sirius-indexers
spec:
  revisionHistoryLimit: 1
  minReadySeconds: 10
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
      maxSurge: 1
  replicas: 1
  selector:
    matchLabels:
      app: indexer-1-worker
  template:
    metadata:
      labels:
        app: indexer-1-worker
      name: indexer-1-worker
    spec:
      containers:
      - name: indexer-1-worker
        image: docker.io/swisschains/sirius-indexer-worker:0.0.23
        imagePullPolicy: Always
        lifecycle:
          postStart:
            exec:
             command:
             - /bin/sh
             - -c 
             - "/bin/echo 'options single-request-reopen' >> /etc/resolv.conf"
        ports:
        - containerPort: 5000
        readinessProbe:
          httpGet:
            path: /api/isalive
            port: 5000
          initialDelaySeconds: 40
          periodSeconds: 10
        livenessProbe:
          httpGet:
            path: /api/isalive
            port: 5000
          initialDelaySeconds: 40
          periodSeconds: 20
        resources:
          requests:
            memory: 256Mi 
            cpu: 1000m 
          limits:
            memory: 1024Mi 
            cpu: 2000m
        env:
        - name: RemoteSettingsRequired
          value: "true"
        - name: RemoteSettingsUrls__0 
          value: "http://settings.sirius.svc.cluster.local/common"
        - name: RemoteSettingsUrls__1
          value: "http://settings.sirius.svc.cluster.local/sirius/common"
        - name: RemoteSettingsUrls__2
          value: "http://settings.sirius.svc.cluster.local/sirius-indexers/common"
        - name: RemoteSettingsUrls__3
          value: "http://settings.sirius.svc.cluster.local/sirius-indexers/indexer-1"
      tolerations:
       - effect: NoSchedule
         operator: Exists
      nodeSelector:
        agentpool: sirindexers
