apiVersion: v1
kind: Service
metadata:
  labels:
    app: vault-hsm-shared
    environment: Development
  name: vault-hsm-shared
  namespace: sirius
spec:
  ports:
  - name: http
    port: 8080
    protocol: TCP
    targetPort: 5000
  selector:
    app: vault-hsm-shared
  sessionAffinity: None
  type: ClusterIP
