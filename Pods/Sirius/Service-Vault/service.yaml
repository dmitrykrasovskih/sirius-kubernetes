apiVersion: v1
kind: Service
metadata:
  labels:
    app: vault
    environment: Development
  name: vault
  namespace: sirius
spec:
  ports:
  - name: http
    port: 8080
    protocol: TCP
    targetPort: 5000
  - name: grpc
    port: 80
    protocol: TCP
    targetPort: 5001
  selector:
    app: vault
  sessionAffinity: None
  type: ClusterIP
