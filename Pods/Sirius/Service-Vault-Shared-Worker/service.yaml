apiVersion: v1
kind: Service
metadata:
  labels:
    app: vault-worker
    environment: Development
  name: vault-worker
  namespace: sirius
spec:
  ports:
  - name: http
    port: 8080
    protocol: TCP
    targetPort: 5000
  selector:
    app: vault-worker
  sessionAffinity: None
  type: ClusterIP
