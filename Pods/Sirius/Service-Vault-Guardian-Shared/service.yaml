apiVersion: v1
kind: Service
metadata:
  labels:
    app: vault-guardian-shared
    environment: Development
  name: vault-guardian-shared
  namespace: sirius
spec:
  ports:
  - name: http
    port: 8080
    protocol: TCP
    targetPort: 5000
  selector:
    app: vault-guardian-shared
  sessionAffinity: None
  type: ClusterIP
