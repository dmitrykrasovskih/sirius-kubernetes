apiVersion: v1
kind: Service
metadata:
  labels:
    app: litecoin-private
    environment: test
  name: litecoin-private
  namespace: sirius-nodes
#  annotations:
#    service.beta.kubernetes.io/azure-load-balancer-internal: "true"
spec:
  ports:
  - name: "80"
    port: 80
    protocol: TCP
    targetPort: 8045
  selector:
    app: nginx-proxy-a
  sessionAffinity: None
  type: ClusterIP
#  type: LoadBalancer
