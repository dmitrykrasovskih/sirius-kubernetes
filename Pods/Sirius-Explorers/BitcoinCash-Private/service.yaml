apiVersion: v1
kind: Service
metadata:
  labels:
    app: bitcoincash-private
    environment: test
  name: bitcoincash-private
  namespace: sirius-explorers
spec:
  ports:
  - name: "http"
    port: 80
    protocol: TCP
    targetPort: 3002
  selector:
    app: bitcoincash-private
  sessionAffinity: None
  type: ClusterIP
#  type: LoadBalancer
