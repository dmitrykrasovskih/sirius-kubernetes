apiVersion: v1
kind: Service
metadata:
  labels:
    app: litecoin-swisschain-private
    environment: dev
  name: litecoin-swisschain-private
  namespace: sirius-explorers
spec:
  ports:
  - name: "http"
    port: 80
    protocol: TCP
    targetPort: 3002
  selector:
    app: litecoin-swisschain-private
  sessionAffinity: None
  type: ClusterIP
#  type: LoadBalancer
